const {
  validateAddBody,
  validateUpdateBody,
  validateFavorite,
} = require("./validateBody");
const isValidId = require("./isValidId");

module.exports = {
  validateAddBody,
  validateUpdateBody,
  isValidId,
  validateFavorite,
};
